
2026.6.25
经典 《Attention Is All Your Need》 Transformer
    论文首次介绍了transformer架构。transformer是一个神经网络模型，不同于RNN对训练的串行性和LSTM对于长序列数据的遗忘特性，
transformer架构可以做到对数据的并行训练，更重要的是模型学到了各个词之间的注意关系。
    transformer单元由一个编码器和一个解码器构成。编码器内，接受输入到多头自注意层，然后经自注意力机制处理后进入线性层，在
线性层输出后进行残差连接防止网络退化，同时归一化得到编码；解码器内，第一层带掩码的多头自注意层接受输出（带掩码是为了防止模
型看到答案，导致训练无效），将其（作为Q）处理后输入到第二个多头自注意层，第二层多头自注意层接受第一层的输出，与编码器的输出
（作为K V）后，将输出给到线性层，处理同上。
    由N个并行的上述transformer单元构成了transformer架构。将待训练数据输入前，需要将其序列化。所以在输入前有一个词Embeddi—
—ng层将数据嵌入；为了标记单词/字在句子中的位置，也使用了位置Embedding，为固定的sin、cos函数。二者相加得到输入序列X。
    其中， Multi-Head Attention 层的工作原理如下：
    自注意单元，主要目的是训练三个矩阵：W_q、W_k、W_v。分别代表查询参数矩阵、键矩阵、值矩阵。初始三个矩阵按照指定方式初始化
，然后将输入矩阵X（每一行代表一个字/单词，假设8*256）分别与W_q、W_k、W_v（维度均为256*8）相乘得到Q、K、V，作为这段序列的查
询Query、键Key、值Value。然后将K^T、Q进行softmax处理，同时为了防止梯度减小，除以√d_k后再与V相乘，得到每个词对自己和其他词的注
意力Attention，然后输出得到Z_1。由多个自注意单元训练不同的W_q、W_k、W_v，得到多个输出Z_i，最后将Z_i拼接，再经过线性层得到与
X相同维度的输出Z。
    本文首次提出了脱离RNN和LSTM进行建模的transformer架构，为后续的语言模型研究开辟了新的方向。


小结（AI）：
    Transformer最大的创新不是QKV，而是完全放弃RNN和CNN，只依赖Attention建模序列关系。
    Self-Attention让序列中任意两个Token能够直接交互，因此能够并行计算。
    Encoder负责理解输入；Decoder负责结合历史输出和Encoder信息生成下一个Token。
    Attention真正计算的是Softmax(QKᵀ/√dk)V。
    Position Encoding补充位置信息，因为Attention本身不知道顺序。
    Multi-head让模型能够从多个不同的表示子空间学习关系。
    整个模型实际上就是不断堆叠相同的Encoder Layer和Decoder Layer。


2026.6.29
前沿 《ICLR-2025-safety-alignment-should-be-made-more-than-just-a-few-tokens-deep-Paper-Conference》
    论文介绍了大模型的安全对齐，指出了当前（2025年）大模型对于违禁词的回答限制存在弊端，容易被简单的手段（如预填充、GCG、解码
参数攻击、微调攻击）绕开安全检测，大模型依旧会输出违禁词。论文指出，大多数模型会在开头的有限token内拒绝提供关于违禁词的回答，
但在后边的深层token时，模型依旧会输出违禁词，作者将此问题定义为 浅层安全对齐 。
    针对这个问题，文章提出两种解决方法供参考：第一种为数据增强，在模型输出了有害内容后能紧急刹车；第二种是增强约束，在模型输出
的前几个token加强梯度，防止其偏离安全限制。


2026.7.04
经典 《Improving Language Understanding by Generative Pre-Training》 GPT
    作者指出，当前（2018）语言模型存在问题：下游的NLP任务依赖大量的人工标注数据，不同方向需要不同的模型，这不仅耗费时间，而且
训练出的模型不能立刻投入使用，成本大。而互联网拥有大量未标注数据，这些数据中同样蕴含着丰富的信息。因此，作者提出了依靠未标注数
据训练一个通用模型，再用标注数据微调模型得到某一方向专用模型的想法。
    GPT1使用了Transformer架构的变种，但仅保留了解码器，同时也采用了掩码机制防止模型看到答案。模型在未标注数据上训练时，优化目
标为预测下一个Token的概率。当得到预训练模型后，微调阶段，模型接受输入，并在以下四种任务分别采用相同的解码器以及不同的线性层转
化：文本分类任务，输出为类别；自然语言推断，输出为推理结果；文本相似度，输出相似度；多答案回答，输出不同答案的概率。
    需要指出，输入文本依旧需要进行词嵌入+位置编码进行向量化后才能进入训练。
    值得注意的是，为了使微调数据集契合GPT1的训练方式（处理连续文本），作者将结构化的微调数据集进行了“遍历处理”，即数据的拼接。
具体而言：分类任务无需处理；自然语言推断任务需要使用“$”将 前提 和 假设 连接；相似度任务由于两个句子顺序无关，用“$”以不同顺序来
连接两个句子分别计算相似度；多答案回答，依照“上下文、问题$答案”的方式连接。
    本文首次验证了了基于Transformer Decoder的生成式预训练能有效迁移到多种下游的NLP任务，证明了“预训练+微调”范式在生成式语言模
型上的有效性，为后续的GPT系列奠定基础。


2026.7.09
经典 《BERT: Pre-training of Deep Bidirectional Transformers for Language Understanding》 BERT
    论文指出，语言模型下游任务的两种方式：feature-based和fine-tuning，都采用的是单向语言建模，如GPT是对下一个的token的预测。而
本文实现了一种双通道建模的语言模型BERT，是基于MLM（masked language model）的微调模型。
    BERT是Transformer架构的变种，只保留了编码器。同时，模型在预训练阶段得到的参数，在微调阶段时，即使是不同的任务也会作为相同
的初始参数进行优化。作者采用了一种方法可以无需顾虑输入格式（如<问题，答案>、<句子，句子>），进而专注训练。具体如下：模型输入接
受两个额外的向量：[CLS] 和 [SEP] 。CLS是分类符，其最终输出用来支持分类任务，SEP是分隔符，用来区分两个句子。
    在预训练阶段，为了达到双通道训练效果，模型随机将15%的输入token进行掩盖[mask]；由于微调阶段没有[mask]标记，于是将掩盖策略改
为：若此token被15%选中，则有80%的概率标记为[mask]，有10%的概率为随机token，有10%的概率为不变。然后用这个修改后的token预测原始
token。对于下一句预测任务，简单使用A+B二分类，训练时，B有50%是A的下一句，有50%不是A的下一句。
    在微调阶段，将预训练阶段的全部参数迁移下来，将输入和输出扔进BERT后，再端对端的微调更新参数。输出端根据[cls]输出，[cls]用于
输出标记文本分类、文本蕴含任务等，输出序列为任务结果预测。
    模型消融实验证明，随着模型的层数、隐层节点数、自注意力机制头数的增加，性能会得到显著提升，但是增加到一定数量后，性能提升就
不是很明显了。
    本文首次提出了基于ransformer Encoder的深层双通道预训练模型。


2026.07.16
经典 《Dense Passage Retrieval for Open-Domain Question Answering》 DPR
    开放问题回答（Open-domain QA）一直是研究的热门，传统方法都是基于稀疏向量的匹配，但是无法解决同义词问题，作者提出了基于稠密向
量检索的方法用来回答问题，该方案实现了语义匹配而非词的匹配。模型采用了BERT的架构以及同样的双通道编码方式。作者指出，开放问题回答
的性能依赖于检索器（Retriever）的召回率好坏，因此提高检索器性能能够显著提高最终问答准确率。
    本质上，DPR是这样一个函数：输入接受问题Q和文档序列D_n，返回值是Top-k最切近问题的文档，k远小于n。模型结构：采用了BERT的双通道
结构（transformer的encoder部分），对输入的问题进行向量化，对给出的文档向量化，问题与文档相似度匹配，输出top-k个最匹配的文档。采用
简单的内积计算相似度，模型采用了“双塔”模式，即用两个encoder分别处理问题的向量化和文档的向量化。
    在训练过程中，为了得到更多的优质负样本，作者采用了以下方法：对于一个询问与多个文档源，文档源中提供一个优优样本，其余的是负样本，
而对负样本做了三种来源处理：1、完全随机；2、传统方法提供的top-k文档但事实上无关；3、数据集中匹配了其他问题Q的文档。节省了大量寻找
负样本的时间。
    本文显著提升了检索准确率，并在开放问题回答领域远远由于传统方法。


2026.07.19
经典 《Retrieval-Augmented Generation for Knowledge-Intensive NLP Tasks》 RAG
    作者指出，现有（2020）的微调语言模型尽管在多数的下游任务表现出了良好的性能，但是依旧存在缺点：无法阐明预测的本质原理；面对未知
问题会产生“幻觉”（即胡编乱造）。原因是微调模型训练的参数是全固定的，即知识固定在了参数里无法方便地更新。所以作者提出了一种固定部分
参数+动态检索知识库的非全参数化方法（即RAG）解决微调模型会产生的幻觉问题。
    模型采用了序列-序列的端对端Transformer架构（输入一句话，输出一句话即为序列-序列，transformer保留了原始架构），输入接受问题
（Question）和潜在文档的序列，DPR检索出Top-k契合文档，再一起输入到BART（结合了BERT和GPT优点的，用Encoder+Decoder的去噪自生成语言
预训练模型），由BART根据问题和检索到的文档生成答案。简而言之，模型采用DPR作为检索器，BART作为生成器。DPR将检索出的文档与问题输入给
BART，最后由BART生成答案。
    为了同时优化检索器和生成器，作者将端到端的最终输出作为优化目标，将DPR检索的文档作为隐变量，继而优化DPR。模型采用了两种不同的
检索策略：RAG+Sequence 和 RAG+Token 。其中，RAG+Sequence表示检索到的每个文档，在生成答案时权重是固定的；RAG+Token表示检索到的每个、
文档，在生成答案的下个Token时，权重是变化的，即根据输出需要变化文档的重要性。
    本文创新性的将参数化记忆（模型参数）和非参数化记忆（外部知识库）进行了结合，提出了RAG的新方法用于问题回答，其性能良好，有望在
NLP任务中广泛应用。



2026.07.22
Agent经典 《ReAct: Synergizing Reasoning and Acting in Language Models》 React
    大语言模型在如今（2023）已经有了一定的推理（Reasoning）能力，但是，只能根据输入生成文本，不可以与外部环境做交互。换言之，如今的
大模型只会思考并回答问题，而不能独立思考并完成一个需要操作的实际任务。因此，本文提出了一种React框架，让大模型实现了独立思考+实际操作
的循环操作，继而赋予大模型可以独立完成外部任务的能力。
    作者指出，当前研究多将复杂任务分解为语言模型进行推理，然后有外部程序执行。这既没有将任务进行高层次的抽象推理，也没有保证工作记忆
的留存，即使有工作记忆的留存也是短期的。因此，作者提出了 reasoning + acting （React）的框架用于完成任务。
    普通的大模型将每一步的观测O+行动A保存，作为自己的上下文Context，这不仅计算量大，而且只思考或只行动会产生幻觉。因此，React将问题
A的定义域扩大为整个语言空间，思考内容以及思维链将不会作为下文的内容，但是会影响模型的下一步行为。即思考过程将会整合信息用于提出行动和
进行下一步的思考。React显式的展示思考过程与行动轨迹，让思考流程的可读性及每一步决策的可靠性具有依据。
    作者在 Hot-PotQA 和 FEVER 两个领域进行了实验，第一个让大模型结合多个维基百科推理回答问题，第二个作者摒弃传统的Q+A的训练方式，只
给大模型Q，让大模型自己检索Answer。介绍了Thinking Only、Acting Only和 Thinking + Acting 的相关工作。
    本文提出了一种简单的思考+行动的综合式大语言模型，实验表明，通过多任务训练，以及加入互补范式如强化学习，可以显著扩大React的应用场
景，提高模型的应用能力。本文广泛使用了"Agent"这个名词。


2026.07.27
Agent经典 《Toolformer: Language Models Can Teach Themselves to Use Tools》
    作者指出，虽然大模型可以在少样本甚至零样本的语言任务上取得优秀成果，但依旧存在如下问题：无法获取即时信息、幻觉、对低质资源难以理
解运用、数学能力低下、时间感知差等。因此作者提出了Toolformer，通过赋予大模型调用外部工具的能力，进而解决这些问题。
    Toolformer是一个自监督的模型，可以接受文本描述作为输入。在处理任务时，由模型自己决定何时（when）查询，如何（how）利用各种外部工
具，通过自主规划，模型可以更加理解工具用法，进而可以适用于多种场景。
    具体而言：给定一个文本输入序列X，首先采样位置i，根据位置i将X分为若干份x_1、x_2...然后每段x得到API查询C：c_1、c_2...，然后根据C
进行API查询，得到查询结果r_1、r_2...，根据查询结果，筛选出能减少预测下一个Token的Loss的查询结果r，将r对应的c，插入其在愿文本的中采样
位置i的后面。本质上是模型自主学习哪个分段对应的API查询最契合原文本中下一Token。
    本文提出一种可以供大模型外部调用的工具Toolformer，通过赋予大模型借用工具（如搜索引擎、计算器、翻译）的能力，可以显著提升其在下游
任务的性能。








